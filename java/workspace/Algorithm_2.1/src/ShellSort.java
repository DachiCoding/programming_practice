
public class ShellSort {
	public static void main(String args[]){
		//ShellSort
		int h;
		int n;
		int[] arr;
		for(int i = h; i < n - h; i++){ // WRONG! i < n
			/* WRONG! Should Implement Insertion Sort Here, with h gap 
			for (int j = i; j < n + i - h ; j+=h){
				if (arr[j] < arr[j-h]) swap(arr[j],arr[j-h]); 
			}
			*/
		}
	}
}
