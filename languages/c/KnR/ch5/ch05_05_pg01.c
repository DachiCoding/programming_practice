#include <stdio.h>

void strcopy(char *s, char *t);

int main(){
 char amessage[] = "now is the time":
 char *pmessage = "now is the time";
}

void strcopy(char *s, char *t){
 while ((*s = *t) != '\0'){
  t++;
  s++;
 }
}
